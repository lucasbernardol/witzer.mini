<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,300;0,400;0,700;1,300&display=swap"
      rel="stylesheet"
    />

    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
    />

    <link rel="stylesheet" href="/styles/global.css" />
    <link rel="stylesheet" href="/styles/pages/home.css" />

    <link rel="stylesheet" href="/styles/components/modal.component.css" />
    <link rel="stylesheet" href="/styles/components/copy.component.css" />

    <% if (href) { %>
    <!-- Modal script -->
    <script defer src="/scripts/main.js" type="text/javascript"></script>
    <% } %>

    <title>Witzer.io | Encurdator de URL's seguro e fácil de usar.</title>
  </head>
  <body>
    <div class="application-wrapper">
      <header class="header-container">
        <p class="header__version">Versão 1.0</p>
      </header>

      <main class="main-container">
        <div class="shortenner">
          <header class="shortenner__header">
            <h1 class="shortenner__title">
              Witzer<span class="title-dot">.</span>dev
            </h1>

            <p class="shortener__text" title="Witzer.dev: about us">
              Encurte seus links através da plataforma witzer. É fácil, rápido e
              seguro.

              <span class="shortener-text__security">
                Não salvamos dados de sigilosos ou algo que pode rastrear você.
              </span>
            </p>
          </header>

          <form action="/shortenings" class="shortener__form" method="POST">
            <fieldset>
              <legend class="screen-reader">Criar URL</legend>

              <div class="input-group">
                <input
                  id="original"
                  type="url"
                  title="Digite uma URL válida"
                  tabindex="1"
                  name="original_url"
                  placeholder="https://www.google.com"
                  maxlength="2048"
                  required
                  autofocus
                  autocomplete="off"
                />

                <div class="link-icon">
                  <img src="/assets/link.svg" alt="Link svg icon" />
                </div>
              </div>
            </fieldset>

            <button class="button" type="submit">Encurtar</button>
          </form>
        </div>
      </main>

      <footer class="fotter-container">
        <h2>Links</h2>
      </footer>
    </div>

    <!-- prettier-ignore -->
    <% if (href) { %>
      <%- include('./components/modal.ejs', {
          hidden: false, 
          href, 
        }) 
      %>
    <% } %>
  </body>
</html>
